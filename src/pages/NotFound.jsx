import React from 'react'

const css = { display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh', fontSize: '24px' }

const NotFound = () => {
  return <div style={css}>Hmm...this page doesn't exist. Try something else.</div>
}

export default NotFound
